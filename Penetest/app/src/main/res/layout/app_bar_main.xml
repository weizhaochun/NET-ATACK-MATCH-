<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="com.ricochet.penetest.MainActivity">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/AppTheme.PopupOverlay" />

    </android.support.design.widget.AppBarLayout>

    <include
        android:id="@+id/include_main"
        android:visibility="visible"
        layout="@layout/content_main" />
    <include
        android:id="@+id/include_start"
        android:visibility="gone"
        layout="@layout/content_start" />
    <include
        android:id="@+id/include_information"
        android:visibility="gone"
        layout="@layout/content_information" />
    <include
        android:id="@+id/include_vulnerability"
        android:visibility="gone"
        layout="@layout/content_vulnerability" />
    <include
        android:id="@+id/include_attack"
        android:visibility="gone"
        layout="@layout/content_attack" />
    <include
        android:id="@+id/include_declaration"
        android:visibility="gone"
        layout="@layout/content_declaration" />
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        android:src="@drawable/add" />

</android.support.design.widget.CoordinatorLayout>
