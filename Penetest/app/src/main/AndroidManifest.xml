<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ricochet.penetest">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".InformationBasic"/>
        <service android:name=".InformationWhois"/>
        <service android:name=".InformationPort"/>
        <service android:name=".VulnerabilityRequest"/>
        <service android:name=".VulnerabilityRead"/>
        <service android:name=".DOS"/>
        <service android:name=".XSS"/>
        <service android:name=".SQL"/>
    </application>

</manifest>